import React from 'react';
import { Formik } from "formik";
import yup from "yup";

import Thumb from "./Thumb";

const dropzoneStyle = {
  width: "100%",
  height: "auto",
  borderWidth: 2,
  borderColor: "rgb(102, 102, 102)",
  borderStyle: "dashed",
  borderRadius: 5,
}

const UploadFiles = () =>(<h1>dgbnsd</h1>);

  export default UploadFiles;